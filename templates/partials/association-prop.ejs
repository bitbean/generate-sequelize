<%- include("./property.ejs", { name, allowNull: !!optional, tsType: type === 'belongsToMany' ? `(${targetName} & { ${throughAlias}: ${through} })[]` : `${targetName}${type === 'hasMany' ? '[]' : ''}` }) %>